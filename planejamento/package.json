{
  "name": "carro-chefe-central-operacional",
  "version": "0.1.0",
  "private": true,
  "description": "Painel e API local para coordenar o planejamento do Carro Chefe.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/store.js && node --check lib/validation.js && node --check public/app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}

